machine:
  services:
    - docker
dependencies:
  override:
    - docker build -t aro-postgis .
    - docker run --name postgres -e POSTGRES_PASSWORD=aro -e POSTGRES_USER=aro -p 5434:5432 -d aro-postgis 

test:
  override:
    - psql -c 'SELECT 1;

